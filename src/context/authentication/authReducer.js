import {
  SUCCES_REGISTER,
  ERROR_REGISTER,
  GET_USER,
  SUCCES_LOGIN,
  CLOSE_SESSION,
} from '../../types'

export default (state, action) => {
  switch (action.type) {
    default:
      return state
  }
}
